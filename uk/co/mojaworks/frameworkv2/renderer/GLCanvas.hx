package uk.co.mojaworks.frameworkv2.renderer;
import flash.geom.Matrix3D;
import openfl.geom.ColorTransform;
import openfl.geom.Matrix;
import openfl.geom.Rectangle;
import openfl.geom.Rectangle;
import openfl.gl.GLBuffer;
import openfl.gl.GLTexture;

/**
 * ...
 * @author Simon
 */
class GLCanvas
{

	public function new() 
	{
		
	}
	
	public function renderImage( texture : GLTexture, vertexData : GLBuffer, transform : Matrix3D, tint : ColorTransform ) {
		
	}
	
	public function renderColour( vertexData : GLBuffer, transform : Matrix3D, colour : ColorTransform ) {
		
	}
	
}
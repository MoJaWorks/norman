package uk.co.mojaworks.frameworkv2.core ;

/**
 * A module is a piece of code that can serve on of two purposes:
 * 
 * - It can act as a proxy to communicate with hardware / external software
 * - It can be a persistent data store that keeps data between screens and throughout the current game session
 * 
 * @author Simon
 */
interface IModule
{
	
}